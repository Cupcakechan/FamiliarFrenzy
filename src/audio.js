/* =========================================================================
   audio.js — PLACEHOLDER (not implemented yet).
   Planned for Phase 7: sfx + music loop, starting only after first player input
   Intentionally empty so the project structure is complete and imports stay
   tidy. Do NOT wire this in until we reach its phase.
   ========================================================================= */

export {}; // empty module
