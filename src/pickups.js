/* =========================================================================
   pickups.js — PLACEHOLDER (not implemented yet).
   Planned for Phase 4: currency drops + (later) health pickups the player collects
   Intentionally empty so the project structure is complete and imports stay
   tidy. Do NOT wire this in until we reach its phase.
   ========================================================================= */

export {}; // empty module
