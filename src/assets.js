/* =========================================================================
   assets.js — PLACEHOLDER (not implemented yet).
   Planned for Phase 7: load + cache sprites/audio, with shape fallbacks if a file is missing
   Intentionally empty so the project structure is complete and imports stay
   tidy. Do NOT wire this in until we reach its phase.
   ========================================================================= */

export {}; // empty module
